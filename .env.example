CONTACT_TO_EMAIL=info@alwayscompliant.in
CONTACT_FROM_EMAIL=website@alwayscompliant.in

# Required: API key from https://resend.com (Dashboard > API Keys). The
# sending domain (alwayscompliant.in) must be verified in Resend first —
# Dashboard > Domains > Add Domain, then add the DNS records it gives you.
RESEND_API_KEY=
